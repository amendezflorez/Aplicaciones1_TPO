<?xml version="1.0" encoding="utf-8"?>
<!-- Cuerpo del diálogo de ofertar: monto obligatorio + mensaje opcional (punto 7). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="8dp">

    <EditText
        android:id="@+id/etOfferAmount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/detail_offer_hint"
        android:inputType="numberDecimal"
        android:importantForAutofill="no" />

    <EditText
        android:id="@+id/etOfferMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:hint="@string/detail_offer_message_hint"
        android:inputType="textCapSentences|textMultiLine"
        android:minLines="1"
        android:importantForAutofill="no" />

</LinearLayout>
